# Welt in Flammen characters for the Central Asian Red Army.

characters = {

	# Deliberately defined with NO country_leader role of its own. The CRA does
	# not exist at game start, so a leader role here would have nothing to
	# attach to; the role is granted with add_country_leader_role when the
	# breakup fires, which is the same pattern wif_sov.1 uses to put Zhukov at
	# the head of the SME.
	CRA_yuri_andropov = {
		name = CRA_yuri_andropov

		portraits = {
			civilian = {
				large = GFX_portrait_CRA_yuri_andropov
			}
			army = {
				large = GFX_portrait_CRA_yuri_andropov
			}
		}
	}

	# The CRA's starting general. Unlike the leader role above, the corps
	# commander role IS declared here rather than granted by effect - a role on
	# an unrecruited character is simply dormant, and recruit_character in the
	# breakup effect brings him in with the role already attached.
	#
	# Skill 2 as asked. The sub-skills lean defensive to match what the traits
	# say about him: an infantry officer who fights in broken ground.
	CRA_vasily_petrov = {
		name = CRA_vasily_petrov

		portraits = {
			army = {
				large = GFX_portrait_CRA_vasily_petrov
				small = GFX_portrait_CRA_vasily_petrov_small
			}
		}

		corps_commander = {
			traits = { infantry_leader hill_fighter trait_mountaineer }
			skill = 2
			attack_skill = 1
			defense_skill = 2
			planning_skill = 1
			logistics_skill = 1
		}
	}
}
